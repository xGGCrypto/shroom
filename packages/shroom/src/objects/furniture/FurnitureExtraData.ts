/**
 * Extra data for a furniture item, such as logic and visualization strings.
 * @category Furniture
 */
export interface FurnitureExtraData {
  /** Visualization string for the furniture (optional). */
  visualization?: string;
  /** Logic string for the furniture (optional). */
  logic?: string;
}
